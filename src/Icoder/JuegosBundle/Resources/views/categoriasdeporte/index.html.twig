{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categoriasdeportes list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Edad</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categoriasDeporte in categoriasDeportes %}
            <tr>
                <td><a href="{{ path('categoria_show', { 'id': categoriasDeporte.id }) }}">{{ categoriasDeporte.id }}</a></td>
                <td>{{ categoriasDeporte.nombre }}</td>
                <td>{{ categoriasDeporte.edad }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('categoria_show', { 'id': categoriasDeporte.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('categoria_edit', { 'id': categoriasDeporte.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categoria_new') }}">Create a new categoriasDeporte</a>
        </li>
    </ul>
{% endblock %}
