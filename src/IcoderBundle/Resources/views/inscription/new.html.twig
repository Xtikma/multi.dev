{% extends 'IcoderBundle:inscription:index.html.twig' %}

{% block panelBody %}
    <h1>Inscription creation</h1>

    {{ dump(inscription) }}
    
    {{ dump(canton) }}
    
    {{ dump(test) }}

    <ul>
        <li>
            <a href="{{ path('inscription_index') }}">Listar Inscripciones</a>
        </li>
    </ul>
{% endblock %}
